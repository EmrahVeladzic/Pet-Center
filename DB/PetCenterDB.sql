CREATE DATABASE [$(DB_NAME)];
GO
ALTER DATABASE [$(DB_NAME)] SET RECURSIVE_TRIGGERS ON;
GO
USE [$(DB_NAME)];
GO

CREATE SCHEMA [File]
GO
CREATE SCHEMA [Person]
GO
CREATE SCHEMA [Business]
GO
CREATE SCHEMA [Offer]
GO
CREATE SCHEMA [Pending]
GO
CREATE SCHEMA [Communication]
GO
CREATE SCHEMA [Animal]
GO
CREATE SCHEMA [Product]
GO
CREATE SCHEMA [Service]
GO

CREATE TABLE [Person].[Account](
    ID UNIQUEIDENTIFIER NOT NULL DEFAULT NEWSEQUENTIALID() PRIMARY KEY,
    Contact VARCHAR(255) NOT NULL,
    PasswordHash VARCHAR(44) NOT NULL,
    PasswordSalt VARCHAR(24) NOT NULL,
    AccessLevel TINYINT NOT NULL,
    Verified BIT NOT NULL
);
GO

CREATE TABLE [File].[Album](
    ID UNIQUEIDENTIFIER NOT NULL DEFAULT NEWSEQUENTIALID() PRIMARY KEY,
    Capacity TINYINT NOT NULL,
    Reserved TINYINT NOT NULL,
	OwnerID UNIQUEIDENTIFIER NOT NULL FOREIGN KEY REFERENCES [Person].[Account](ID)	
);
GO  
    
CREATE TABLE [File].[Image](
    ID UNIQUEIDENTIFIER NOT NULL DEFAULT NEWSEQUENTIALID() PRIMARY KEY,
    AlbumID UNIQUEIDENTIFIER NOT NULL,
    Width SMALLINT NOT NULL,
    Height SMALLINT NOT NULL,
    ImageData VARCHAR(MAX) NOT NULL,

    CONSTRAINT FK_Image_Album FOREIGN KEY (AlbumID) REFERENCES [File].[Album](ID) ON DELETE CASCADE
);
GO

CREATE TABLE [Person].[User](
    ID UNIQUEIDENTIFIER NOT NULL DEFAULT NEWSEQUENTIALID() PRIMARY KEY,
    AccountID UNIQUEIDENTIFIER NOT NULL FOREIGN KEY REFERENCES [Person].[Account](ID),
    UserName NVARCHAR(50) NOT NULL,
    ProfilePictureID UNIQUEIDENTIFIER NOT NULL FOREIGN KEY REFERENCES [File].[Album](ID),

    CONSTRAINT UQ_UserName UNIQUE (UserName),
	CONSTRAINT UQ_User_Album UNIQUE (ProfilePictureID)
);
GO

CREATE TABLE [Business].[Franchise](
    ID UNIQUEIDENTIFIER NOT NULL DEFAULT NEWSEQUENTIALID() PRIMARY KEY,
    OwnerID UNIQUEIDENTIFIER NOT NULL FOREIGN KEY REFERENCES [Person].[User](ID),
    FranchiseName NVARCHAR(50) NOT NULL,
    LogoID UNIQUEIDENTIFIER NOT NULL FOREIGN KEY REFERENCES [File].[Album](ID),
    DefaultContact VARCHAR(255) NOT NULL,

	CONSTRAINT UQ_Franchise_Album UNIQUE (LogoID)
);
GO

CREATE TABLE [Business].[Facility](
    ID UNIQUEIDENTIFIER NOT NULL DEFAULT NEWSEQUENTIALID() PRIMARY KEY,
    FranchiseID UNIQUEIDENTIFIER NOT NULL FOREIGN KEY REFERENCES [Business].[Franchise](ID),
    City NVARCHAR(100) NOT NULL,
    Street NVARCHAR(150) NOT NULL,
    Contact VARCHAR(255),
);
GO

CREATE TABLE [Business].[FormTemplate](
    ID UNIQUEIDENTIFIER NOT NULL DEFAULT NEWSEQUENTIALID() PRIMARY KEY,
    FormDescription NVARCHAR(100) NOT NULL
);
GO

CREATE TABLE [Business].[FormTemplateField](
    ID UNIQUEIDENTIFIER NOT NULL DEFAULT NEWSEQUENTIALID() PRIMARY KEY,
    FormTemplateID UNIQUEIDENTIFIER NOT NULL FOREIGN KEY REFERENCES [Business].[FormTemplate](ID),
    FormFieldDescription NVARCHAR(50) NOT NULL,
    DataType TINYINT NOT NULL,
    Optional BIT NOT NULL,
);
GO

CREATE TABLE [Pending].[Registration](
    ID UNIQUEIDENTIFIER NOT NULL DEFAULT NEWSEQUENTIALID() PRIMARY KEY,
    AccountID UNIQUEIDENTIFIER NOT NULL,
    Code INT NOT NULL,
    Expiry DATETIME2 NOT NULL,
    NextAttempt DATETIME2 NOT NULL,

    CONSTRAINT FK_Registration_Account FOREIGN KEY (AccountID) REFERENCES [Person].[Account](ID) ON DELETE CASCADE
);
GO

CREATE TABLE [Pending].[Form](
    ID UNIQUEIDENTIFIER NOT NULL DEFAULT NEWSEQUENTIALID() PRIMARY KEY,
    FormTemplateID UNIQUEIDENTIFIER NOT NULL FOREIGN KEY REFERENCES [Business].[FormTemplate](ID),
    UserID UNIQUEIDENTIFIER NOT NULL FOREIGN KEY REFERENCES [Person].[User](ID),
    AlbumID UNIQUEIDENTIFIER NOT NULL FOREIGN KEY REFERENCES [File].[Album](ID),
    Expiry DATETIME2 NOT NULL,

	CONSTRAINT UQ_Form_Album UNIQUE (AlbumID)
);
GO

CREATE TABLE [Pending].[FormFieldEntry](
    ID UNIQUEIDENTIFIER NOT NULL DEFAULT NEWSEQUENTIALID() PRIMARY KEY,
    FormID UNIQUEIDENTIFIER NOT NULL FOREIGN KEY REFERENCES [Pending].[Form](ID),
    FormTemplateFieldID UNIQUEIDENTIFIER NOT NULL FOREIGN KEY REFERENCES [Business].[FormTemplateField](ID),
    Serialized VARCHAR(255) NOT NULL,
);
GO

CREATE TABLE [Animal].[Kind](
    ID UNIQUEIDENTIFIER NOT NULL DEFAULT NEWSEQUENTIALID() PRIMARY KEY,
    Title NVARCHAR(30) NOT NULL,

	CONSTRAINT UQ_Kind_Album UNIQUE (AlbumID)
);
GO

CREATE TABLE [Animal].[Breed](
    ID UNIQUEIDENTIFIER NOT NULL DEFAULT NEWSEQUENTIALID() PRIMARY KEY,
    KindID UNIQUEIDENTIFIER NOT NULL FOREIGN KEY REFERENCES [Animal].[Kind](ID),   
    Title NVARCHAR(30) NOT NULL,
    Scale TINYINT NOT NULL,
    Diet TINYINT NOT NULL,
    Investment REAL NOT NULL,
    Territory REAL NOT NULL,
    Pricing REAL NOT NULL,
    Longevity REAL NOT NULL,
    Cohabitation REAL NOT NULL,
    AlbumID UNIQUEIDENTIFIER NOT NULL FOREIGN KEY REFERENCES [File].[Album](ID),

	CONSTRAINT UQ_Breed_Album UNIQUE (AlbumID)
);
GO

CREATE TABLE [Animal].[Individual](
    ID UNIQUEIDENTIFIER NOT NULL DEFAULT NEWSEQUENTIALID() PRIMARY KEY,
    BreedID UNIQUEIDENTIFIER NOT NULL FOREIGN KEY REFERENCES [Animal].[Breed](ID),
    AnimalName NVARCHAR(30) NOT NULL,
    BirthDate DATETIME2 NOT NULL,
    AlbumID UNIQUEIDENTIFIER NOT NULL FOREIGN KEY REFERENCES [File].[Album](ID),
    Sex BIT NOT NULL,

	CONSTRAINT UQ_Individual_Album UNIQUE (AlbumID)
);
GO

CREATE TABLE [Service].[MedicalProcedure](
    ID UNIQUEIDENTIFIER NOT NULL DEFAULT NEWSEQUENTIALID() PRIMARY KEY,
    ProcedureDescription NVARCHAR(100) NOT NULL
);
GO

CREATE TABLE [Service].[MedicalProcedureSpecification](
    ID UNIQUEIDENTIFIER NOT NULL DEFAULT NEWSEQUENTIALID() PRIMARY KEY,
    ProcedureID UNIQUEIDENTIFIER NOT NULL FOREIGN KEY REFERENCES [Service].[MedicalProcedure](ID),
    KindID UNIQUEIDENTIFIER NOT NULL FOREIGN KEY REFERENCES [Animal].[Kind](ID),
    BreedID UNIQUEIDENTIFIER FOREIGN KEY REFERENCES [Animal].[Breed](ID),
    ApproximateAgeDays INT,
    SexSpecific BIT,
    Optional BIT NOT NULL,
);
GO

CREATE TABLE [Animal].[MedicalRecordEntry](
    ID UNIQUEIDENTIFIER NOT NULL DEFAULT NEWSEQUENTIALID() PRIMARY KEY,
    ProcedureID UNIQUEIDENTIFIER NOT NULL FOREIGN KEY REFERENCES [Service].[MedicalProcedure](ID),
    AnimalID UNIQUEIDENTIFIER NOT NULL FOREIGN KEY REFERENCES [Animal].[Individual](ID),
    Notes NVARCHAR(100) NOT NULL,
    DatePerformed DATETIME2 NOT NULL,
);
GO

CREATE TABLE [Product].[Category](
    ID UNIQUEIDENTIFIER NOT NULL DEFAULT NEWSEQUENTIALID() PRIMARY KEY,
    Title NVARCHAR(50) NOT NULL,
    Consumable BIT NOT NULL,
);
GO

CREATE TABLE [Product].[Item](
    ID UNIQUEIDENTIFIER NOT NULL DEFAULT NEWSEQUENTIALID() PRIMARY KEY,
    Title NVARCHAR(50) NOT NULL,
    AlbumID UNIQUEIDENTIFIER NOT NULL FOREIGN KEY REFERENCES [File].[Album](ID),
    CategoryID UNIQUEIDENTIFIER NOT NULL FOREIGN KEY REFERENCES [Product].[Category](ID),
    TargetKind UNIQUEIDENTIFIER FOREIGN KEY REFERENCES [Animal].[Kind](ID),
    TargetScale TINYINT,
    MassGrams INT,
);
GO

CREATE TABLE [Offer].[Listing](
    ID UNIQUEIDENTIFIER NOT NULL DEFAULT NEWSEQUENTIALID() PRIMARY KEY,
    ListingName NVARCHAR(50) NOT NULL,
    ListingDescription NVARCHAR(1000) NOT NULL,
    FranchiseID UNIQUEIDENTIFIER NOT NULL FOREIGN KEY REFERENCES [Business].[Franchise](ID),
    AlbumID UNIQUEIDENTIFIER NOT NULL FOREIGN KEY REFERENCES [File].[Album](ID),
    PriceMinor BIGINT NOT NULL,
    ListingType TINYINT NOT NULL,
    Approved BIT NOT NULL,
    Expiry DATETIME2 NOT NULL,
	CONSTRAINT UQ_Listing_Album UNIQUE (AlbumID)
);
GO

CREATE TABLE [Offer].[Available](
    ID UNIQUEIDENTIFIER NOT NULL DEFAULT NEWSEQUENTIALID() PRIMARY KEY,
    ListingID UNIQUEIDENTIFIER NOT NULL FOREIGN KEY REFERENCES [Offer].[Listing](ID),
    FacilityID UNIQUEIDENTIFIER NOT NULL FOREIGN KEY REFERENCES [Business].[Facility](ID),
    Stock TINYINT,
);
GO

CREATE TABLE [Offer].[ProductListing](
    ID UNIQUEIDENTIFIER NOT NULL PRIMARY KEY FOREIGN KEY REFERENCES [Offer].[Listing](ID),
    ProductID UNIQUEIDENTIFIER NOT NULL FOREIGN KEY REFERENCES [Product].[Item](ID),
);
GO

CREATE TABLE [Offer].[AnimalListing](
    ID UNIQUEIDENTIFIER NOT NULL PRIMARY KEY FOREIGN KEY REFERENCES [Offer].[Listing](ID),
    AnimalID UNIQUEIDENTIFIER NOT NULL FOREIGN KEY REFERENCES [Animal].[Individual](ID),
);
GO

CREATE TABLE [Offer].[MedicalListing](
    ID UNIQUEIDENTIFIER NOT NULL PRIMARY KEY FOREIGN KEY REFERENCES [Offer].[Listing](ID),
    ProcedureID UNIQUEIDENTIFIER NOT NULL FOREIGN KEY REFERENCES [Service].[MedicalProcedure](ID),
);

CREATE TABLE [Communication].[Comment](
    ID UNIQUEIDENTIFIER NOT NULL DEFAULT NEWSEQUENTIALID() PRIMARY KEY,
    Contents NVARCHAR(1000) NOT NULL,
    PosterID UNIQUEIDENTIFIER NOT NULL FOREIGN KEY REFERENCES [Person].[User](ID),
    Creation DATETIME2 NOT NULL DEFAULT SYSUTCDATETIME(),
    ListingID UNIQUEIDENTIFIER NOT NULL FOREIGN KEY REFERENCES [Offer].[Listing](ID),
);
GO

CREATE TABLE [Business].[EmployeeRecord](
    ID UNIQUEIDENTIFIER NOT NULL DEFAULT NEWSEQUENTIALID() PRIMARY KEY,
    UserID UNIQUEIDENTIFIER NOT NULL FOREIGN KEY REFERENCES [Person].[User](ID),
    FranchiseID UNIQUEIDENTIFIER NOT NULL FOREIGN KEY REFERENCES [Business].[Franchise](ID),
);
GO

CREATE TABLE [Offer].[Wishlist](
    ID UNIQUEIDENTIFIER NOT NULL DEFAULT NEWSEQUENTIALID() PRIMARY KEY,
    UserID UNIQUEIDENTIFIER NOT NULL FOREIGN KEY REFERENCES [Person].[User](ID),
    Term NVARCHAR(50) NOT NULL,

    CONSTRAINT FK_Wishlist_User FOREIGN KEY(UserID) REFERENCES [Person].[User](ID) ON DELETE CASCADE
);
GO

CREATE TABLE [Communication].[Notification](
    ID UNIQUEIDENTIFIER NOT NULL DEFAULT NEWSEQUENTIALID() PRIMARY KEY,
    UserID UNIQUEIDENTIFIER NOT NULL FOREIGN KEY REFERENCES [Person].[User](ID),
    Title NVARCHAR(50) NOT NULL,
    Body NVARCHAR(255) NOT NULL,
    ListingID UNIQUEIDENTIFIER FOREIGN KEY REFERENCES [Offer].[Listing](ID),
    Expiry DATETIME2 NOT NULL,
);
GO

CREATE TABLE [Communication].[Announcement](
    ID UNIQUEIDENTIFIER NOT NULL DEFAULT NEWSEQUENTIALID() PRIMARY KEY,
    RoleSpecific TINYINT,
    Title NVARCHAR(50) NOT NULL,
    Body NVARCHAR(255) NOT NULL,
    Expiry DATETIME2 NOT NULL,
);
GO

CREATE TABLE [Communication].[Report](
    ID UNIQUEIDENTIFIER NOT NULL DEFAULT NEWSEQUENTIALID() PRIMARY KEY,
    CommentID UNIQUEIDENTIFIER FOREIGN KEY REFERENCES [Communication].[Comment](ID),
    ListingID UNIQUEIDENTIFIER NOT NULL FOREIGN KEY REFERENCES [Offer].[Listing](ID),
    Reason NVARCHAR(100) NOT NULL,
    Expiry DATETIME2 NOT NULL
);
GO

CREATE TABLE [Animal].[Usage](
    ID UNIQUEIDENTIFIER NOT NULL DEFAULT NEWSEQUENTIALID() PRIMARY KEY,
    KindID UNIQUEIDENTIFIER NOT NULL FOREIGN KEY REFERENCES [Animal].[Kind](ID),
    ScaleSpecific TINYINT,
    CategoryID UNIQUEIDENTIFIER NOT NULL FOREIGN KEY REFERENCES [Product].[Category](ID),
    AverageDailyAmountGrams INT NOT NULL,
);
GO